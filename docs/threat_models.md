# Threat Models & Detection Methodologies: AEGIS Platform

This document specifies the 7 cyber threat categories analyzed by the AEGIS passive detection engine in unidirectional network monitoring environments.

All detection models operate strictly on passively observed packet headers, transport-layer framing, and temporal flow statistics. Zero packet injection, active handshakes, or payload decryption are used.

---

## 1. Volumetric & Protocol DDoS

- **Threat Description**: Distributed denial of service attempts aimed at overwhelming enclave servers or critical infrastructure endpoints.
- **Specialized Attack Subtypes**:
  1. **TCP SYN Floods**: Mass transmission of TCP SYN packets with no subsequent ACKs (`syn_ack_ratio > 10.0`), exhausting kernel TCP connection state tables.
  2. **High-Rate UDP Floods**: Volumetric floods saturating network interfaces with high packet arrival rates (`udp_rate > 80.0` pkts/sec).
  3. **UDP Amplification & Reflection**: Exploitation of misconfigured third-party servers on known reflective UDP ports:
     - DNS (UDP 53)
     - NTP (UDP 123)
     - SSDP (UDP 1900)
     - Memcached (UDP 11211)
     - LDAP (UDP 389)
     - *Detection signature*: Byte amplification ratio $\frac{\text{response\_bytes}}{\text{request\_bytes}} > 10.0$ and substantial byte volume.
  4. **Spoofed-Source Flooding**: Volumetric floods where the attacker randomizes source IP addresses to evade rate-limiting.
     - *Detection signature*: High source-IP Shannon entropy ($H_{src} \ge 3.5$ bits) and low Herfindahl concentration ($< 0.35$) coupled with elevated packet arrival rates.
- **Explainable Evidence Output**:
  ```json
  {
    "attack_subtype": "SPOOFED_SOURCE_FLOOD",
    "syn_rate": 824.0,
    "source_ip_entropy": 3.82,
    "herfindahl_index": 0.08,
    "syn_ack_ratio": 45.2,
    "unique_sources": 142
  }
  ```

---

## 2. Botnet C2 Beaconing (AEGIS v2 Adversarially Robust)

- **Threat Description**: Periodic heartbeats, check-in polling loops, or automated C2 tasks generated by compromised internal hosts connecting to remote Command & Control (C2) servers (e.g. Cobalt Strike, Sliver, Mythic, Trickbot).
- **Passive Indicators (Multi-Feature Periodicity Analysis)**:
  - **Inter-Arrival Time (IAT) Sequence**: Timestamps of consecutive connections from a host to the same external destination.
  - **Coefficient of Variation ($CV = \sigma / \mu$)**: Dispersion metric where low $CV$ indicates strict intervals. CV is one bounded piece of evidence, not an isolated hard decision.
  - **Multi-Lag Autocorrelation**: Evaluates Pearson autocorrelation across a bounded lag search ($k \in [1, 5]$). Measures residual periodic structure even when implants utilize structured or alternating sleep delays (e.g., alternating 8s / 14s intervals).
  - **Spectral Periodicity (Lightweight NumPy FFT)**: Ratio of maximum harmonic peak power to total non-DC power in the IAT sequence, identifying periodic harmonics without external heavy DSP frameworks.
  - **Jitter Magnitude & Jitter-Aware Scoring**: Measures consecutive interval difference (MACD) and attenuates timing confidence under heavy random dispersion to avoid false certainty.
  - **IAT Histogram Entropy**: Measures dispersion irregularity across interval bins.
  - **Destination Persistence vs. Rarity**: Distinguishes repeated communication to the same specific destination (persistence) from whether the destination is novel/rare across the monitored enclave baseline. Legitimate public resolvers (e.g. 8.8.8.8) are suppressed unless accompanied by independent suspicious protocol metadata.
- **Explainable Evidence Output**:
  ```json
  {
    "threat_diagnosis": "Periodic C2 beaconing pattern detected (Mean IAT: 10.00s, CV: 0.0000, Best Lag: 1, Autocorr: 1.00).",
    "recent_iat_pattern": "10.0s, 10.0s, 10.0s, 10.0s, 10.0s",
    "mean_inter_arrival_sec": 10.0,
    "std_inter_arrival_sec": 0.0,
    "coefficient_of_variation": 0.0,
    "periodicity_score": 0.98,
    "lag1_autocorrelation": 1.0,
    "best_lag": 1,
    "best_autocorrelation": 1.0,
    "spectral_periodicity": 1.0,
    "jitter_magnitude": 0.0,
    "jitter_score": 0.0,
    "iat_entropy": 0.0,
    "observed_connection_count": 8,
    "destination_concentration": 1.0,
    "destination_persistence_score": 0.95,
    "destination_rarity_score": 0.85,
    "destination_rarity_confirmed": true
  }
  ```

---

## 3. DGA (Domain Generation Algorithm) Detection

- **Threat Description**: Algorithmic generation of pseudo-random domain names used by malware for resilient C2 rendezvous, circumventing static domain blacklists.
- **Passive Indicators**:
  - **High Shannon Entropy**: Elevated character entropy ($> 3.6$ bits/char) indicating high randomness.
  - **Lexical Structural Anomalies**: High digit ratio ($> 0.30$), low vowel ratio ($< 0.18$), and long consonant clusters (e.g. `x7k29a8d91b4mz09.biz`).
  - **Supervised Random Forest Classifier**: Evaluates multi-dimensional lexical n-gram and character distributions to output class probabilities.
- **Explainable Evidence Output**:
  ```json
  {
    "domain": "x7k29a8d91b4mz09.biz",
    "domain_entropy": 3.8842,
    "domain_length": 20,
    "digit_ratio": 0.4737,
    "vowel_ratio": 0.1053,
    "ml_probability": 0.9820
  }
  ```

---

## 4. DNS Tunnelling & Covert Channels

- **Threat Description**: Covert data exfiltration and bidirectional command encapsulation hidden within DNS request/response payloads (e.g. Iodine, DNScapy, Cobalt Strike DNS listeners).
- **Passive Indicators**:
  - **Subdomain Length & Entropy**: Large payload chunking into subdomain labels ($> 24$ characters) with high Shannon entropy ($> 3.75$ bits/char).
  - **Rolling Record-Type Distribution**: Sliding-window tracking of query types under an apex domain. Abnormal concentrations of TXT queries ($> 40\%$) or NULL queries ($> 10\%$) signal covert channel encapsulation.
  - **Apex Query Rate & Cardinality**: Abnormally high query frequencies ($> 8.0$ queries/sec) directed at an apex with numerous distinct pseudo-random subdomains.
- **Explainable Evidence Output**:
  ```json
  {
    "apex_domain": "exfil-tunnel.org",
    "subdomain": "a8f93e2b109c4d87e65fa31298c",
    "subdomain_length": 27,
    "subdomain_entropy": 4.1250,
    "txt_record_ratio": 0.65,
    "null_record_ratio": 0.15,
    "rolling_query_count": 48
  }
  ```

---

## 5. Encrypted Traffic Threats (TLS / QUIC)

- **Strict Guarantee**: ZERO PAYLOAD DECRYPTION.
- **Threat Description**: C2 communication and malicious activity tunneled over standard encrypted protocols (HTTPS / TLS 1.3 / HTTP/3 over QUIC).
- **Passive Indicators**:
  - **JA3 / JA4 Fingerprinting**: Unencrypted Client Hello cryptographic negotiation strings matched against cataloged malware profiles (Cobalt Strike, Metasploit, AsyncRAT).
  - **SNI Absence or Mismatch**: TLS sessions over port 443 lacking Server Name Indication or presenting direct IP targets.
  - **Sequence of Packet Lengths and Times (SPLT)**: Low variance and characteristic small packet sizes ($50\text{--}250$ bytes) indicating command polling rather than interactive web browsing.
  - **QUIC Framing Inspection (RFC 9000)**: Passive extraction of Long vs. Short headers, Initial/Handshake packet counts, and connection ID lengths over UDP port 443.
- **Explainable Evidence Output**:
  ```json
  {
    "suspicious_ja3_match": true,
    "ja3_hash": "e7d705a3286e19ea42f587b344ee6865",
    "ja4_fingerprint": "t13d1516h2_8daaf6152771_e8f1e7e78f70",
    "is_quic": false,
    "splt_mean_length": 142.5,
    "splt_length_variance": 18.2,
    "outbound_inbound_ratio": 12.8,
    "zero_decryption_verified": true
  }
  ```

---

## 6. Reconnaissance & Network Probing

- **Threat Description**: Initial adversary reconnaissance attempting to discover active hosts and open service ports across the monitored subnet.
- **Attack Subtypes**:
  - **Horizontal Scans**: Single host scanning a single port across dozens of distinct destination IPs in a short window.
  - **Vertical Scans**: Single host scanning dozens of distinct destination ports on a single IP target.
  - **SYN-Only Probes**: Connections consisting solely of SYN packets without subsequent application data transfer (`is_syn_only == True`, `duration < 0.5`s).
- **Explainable Evidence Output**:
  ```json
  {
    "scan_type": "HORIZONTAL_SWEEP",
    "unique_destination_hosts": 38,
    "unique_destination_ports": 1,
    "connections_per_second": 24.5,
    "destination_entropy": 4.82,
    "syn_only_ratio": 0.95
  }
  ```

---

## 7. Data Exfiltration

- **Threat Description**: Unauthorized transfer of sensitive documents, database dumps, or intellectual property across monitored enclave boundaries.
- **Detection Method**: AEGIS employs a dual-path detection engine that evaluates both single-flow volumetric exfiltration (Bulk) and stateful rolling temporal patterns (Slow-and-Low) without decrypting payload data.

### 7.1 Bulk / High-Volume Data Exfiltration

- **Attack Profile**: Adversary performs single-session bulk staging or rapid archival uploads that overwhelm typical outbound baselines.
- **Multi-Signal Passive Indicators**:
  - **Asymmetric Volume**: Outbound-to-inbound byte ratio exceeding configured threshold ($\ge 6.0:1$) with minimum outbound bytes ($\ge 200\text{ KB}$). Safe bounded ratio prevents division-by-zero on pure egress feeds.
  - **Sustained Outbound Rate**: Outbound throughput exceeding configured threshold ($\ge 1.0\text{ MB/s}$) over minimum duration ($\ge 0.5\text{s}$) with directional bias ($\text{ratio} \ge 2.0$) to distinguish exfiltration from balanced duplex downloads.
  - **Large Burst Volume**: Outbound byte volume exceeding burst threshold ($\ge 2.0\text{ MB}$) with directional asymmetry ($\text{ratio} \ge 2.0$).
  - **Destination Persistence**: Repeated connections or persistent flow initiation to the same external destination.
  - **Unsupervised Anomaly Scoring**: Isolation Forest model scoring on flow byte volume, rates, and duration.
- **Subtype Output**: Emitted as `subtype: "BULK"` with confidence bounded between $0.50$ and $0.98$:
  $$\text{Confidence} = \text{Base} (0.65) + \text{Asymmetry} (\le 0.12) + \text{Volume} (\le 0.10) + \text{Rate} (\le 0.08) + \text{Persistence} (\le 0.05) + \text{ML Anomaly} (\le 0.06)$$
- **Explainable Evidence Output**:
  ```json
  {
    "subtype": "BULK",
    "threat_diagnosis": "Upload ratio (1171.77x) exceeds asymmetry threshold (6.0x) with 503.5 KB transferred; Unsupervised Isolation Forest anomaly score (0.729) exceeds threshold",
    "bytes_out": 515580,
    "bytes_in": 440,
    "out_in_ratio": 1171.77,
    "outbound_inbound_ratio": 1171.77,
    "outbound_rate_bytes_per_sec": 168683.13,
    "duration_sec": 3.06,
    "sustained_duration_sec": 3.06,
    "destination_frequency": 1,
    "ratio_threshold_configured": 6.0,
    "ml_anomaly_detected": true
  }
  ```

### 7.2 Slow-and-Low Stateful Data Exfiltration

- **Attack Profile**: Sophisticated adversaries evade single-flow volumetric thresholds by "drip-feeding" data: staging small, sub-threshold transfers (e.g. 8–12 KB chunks) across minutes or hours to a consistent external IP/port, often with deliberate interval jitter.
- **Stateful Multi-Window Architecture**:
  - The detector maintains an in-memory, time-bounded deque (`source_history`) per internal source IP, capped at 1,000 entries with automatic eviction beyond the maximum evaluation horizon (3,600s).
  - Evaluates sliding temporal horizons: **60s**, **300s**, **900s**, and **3600s**.
  - Deduplicates active vs expired flow records via `flow_id` to prevent multi-counting across sampling passes.
- **Multi-Signal Confluence Heuristics**:
  - **Transfer Count & Cumulative Volume**: Requires at least `minimum_transfers` ($\ge 4$) and cumulative outbound volume exceeding baseline ($\ge 35\text{ KB}$).
  - **Destination Persistence ($P$)**: Evaluates the concentration of transfers to the dominant external destination:
    $$P = \frac{\text{Transfers to Dominant Dest}}{\text{Total Transfers in Window}} \ge 0.75$$
  - **Directional Asymmetry Ratio ($R$)**: Ratio of cumulative forward bytes to cumulative backward bytes across the window:
    $$R = \frac{\sum B_{out}}{\max(1, \sum B_{in})} \ge 3.5$$
    *(Safe zero-inbound bound: when $B_{in} \le 0$, ratio is clamped safely to $\min(10000.0, B_{out})$ without division-by-zero or NaN).*
  - **Inter-Arrival Time Regularity ($CV$)**: Measures timing consistency with jitter tolerance:
    $$CV = \frac{\sigma_{IAT}}{\mu_{IAT}} \le 0.50$$
  - **Active Duration ($\Delta t$)**: Timespan from initial to final staged transfer in the active window.
- **Anti-False-Positive Boundaries (Strict Negative Guarding)**:
  - **Periodicity alone NEVER equals exfiltration**: Legitimate telemetry polling, health check-ins, or background updates exhibit low $CV$ ($\le 0.05$). However, legitimate polling produces balanced bidirectional duplex volume (e.g. request + response payload) and balanced byte ratios ($R \approx 1.0$).
  - **Suppression Gate**: If cumulative asymmetry ratio $R < 2.0$ or destination persistence $P < 0.60$, timing regularity is strictly suppressed from contributing to threat score.
- **Scoring & Confidence**:
  Normalized linear combination of volume, asymmetry, persistence, timing regularity, and transfer frequency:
  $$\text{Score} = w_{vol} S_{vol} + w_{asym} S_{asym} + w_{pers} S_{pers} + w_{time} S_{time} + w_{freq} S_{freq}$$
  Alerts fire when $\text{Score} \ge 0.65$ with `subtype: "SLOW_AND_LOW"`.
- **Explainable Evidence Output**:
  ```json
  {
    "subtype": "SLOW_AND_LOW",
    "threat_diagnosis": "Slow-and-low exfiltration: 6 staged transfers (57.6 KB) to persistent destination 203.0.113.90:443 over 70.0s window (ratio: 18.6x, persistence: 100.0%, IAT CV: 0.14)",
    "active_window_sec": 300,
    "total_transfers_in_window": 6,
    "cumulative_bytes_out": 57600,
    "cumulative_bytes_in": 3100,
    "cumulative_asymmetric_ratio": 18.58,
    "destination_persistence_ratio": 1.0,
    "dominant_destination": "203.0.113.90:443",
    "mean_iat_sec": 14.0,
    "std_iat_sec": 1.96,
    "iat_cv": 0.14,
    "slow_exfiltration_score": 0.885,
    "active_duration_sec": 70.0
  }
  ```

- **Prototype Scope Notice**: Thresholds are configurable prototype parameters, not universal security constants. Detection indicates behavioral alignment with exfiltration heuristics rather than absolute attribution.

---

## 8. Threat Fusion & Composite Classification

Individual detector findings are synthesized by the **Threat Fusion Engine (`app/fusion/threat_fusion.py`)**:

### Multi-Vector Correlated Threats
When multiple distinct detectors fire for the same endpoints within a 25-second correlation window, composite alerts are produced with elevated confidence:
- `SUSPICIOUS_ENCRYPTED_TRAFFIC` + `DATA_EXFILTRATION` $\rightarrow$ `POSSIBLE_ENCRYPTED_EXFILTRATION` (Confidence: 0.95, Severity: CRITICAL)
- `C2_BEACONING` + `DNS_TUNNEL` $\rightarrow$ `SUSPICIOUS_C2_OVER_DNS` (Confidence: 0.95, Severity: CRITICAL)
- `RECON_SCAN` + `DDOS_ATTACK` $\rightarrow$ `PROBE_BEFORE_ATTACK` (Confidence: 0.90, Severity: HIGH)

### Unambiguous Fallback Classifications
- **`UNKNOWN_ANOMALY`**: Generated when ML anomaly models (e.g. Isolation Forest) or multivariate flow anomalies flag a session as statistically deviant, but no deterministic rule or signature matches. Rather than discarding or silently misclassifying the session, AEGIS presents it as an `UNKNOWN_ANOMALY` for analyst triage.
- **`INSUFFICIENT_EVIDENCE`**: Assigned when an alert candidate is generated but the session lacks minimum flow duration ($< 1.0$s) or minimum packet threshold ($< 4$ packets) necessary for statistical significance, preventing spurious low-evidence alarms.

---

## 9. Adversarial Beaconing, Validation & Detection Boundaries (AEGIS v2.2)

### DEMONSTRATED Capabilities (Empirically Verified via 38-Scenario Red-Team Suite)
The following capabilities are mathematically and empirically demonstrated in AEGIS:
1. **Structured Jitter Resilience**: Detection of non-uniform, multi-interval C2 schedules (e.g. alternating delays, lag-2 periodicity) via multi-lag autocorrelation ($k \in [1, 5]$) and spectral FFT concentration.
2. **Low-and-Slow Persistent C2 Accumulation**: Incremental detection of long-interval beacons (60s+) using sliding-window destination persistence and host-pair concentration without excessive memory usage.
3. **Multi-Signal Evidence Fusion**: Recovery of high-confidence detection when timing periodicity is randomized ($\ge 40\%$ jitter) if independent observable signals (DGA queries, JA3 hashes, QUIC framing, high destination concentration) are present.
4. **Strict Passive-Only Guarantee**: 100% read-only monitoring with zero active socket transmissions, zero network probing, zero firewall modifications, and zero payload decryption.
5. **Bounded, Honest Confidence Routing**: Degradation of timing confidence under heavy dispersion, routing ambiguous flow profiles to `UNKNOWN_ANOMALY` or `INSUFFICIENT_EVIDENCE` rather than asserting false certainty.
6. **Label Isolation**: Absolute invariance of detector inference to ground-truth labels, scenario metadata, or generator fields.

### NOT DEMONSTRATED / Operational Limitations (What AEGIS Does NOT Claim)
1. **No Universal C2 Detection**: Pure timing-only detection CANNOT detect C2 implants utilizing high-entropy, exponential random sleep distributions ($\ge 60\%$ jitter) in the absence of independent protocol or destination signals.
2. **No Payload Decryption or Content Malware Identification**: AEGIS does not inspect, decrypt, or assemble encrypted application payloads. Detection is purely metadata- and behavior-derived.
3. **No 0% False Positive Rate Guarantee on Benign Periodic Traffic**: Legitimate automated infrastructure (e.g. database keepalives, NTP sync, load-balancer health probes, cloud API polling) exhibits periodic timing and repeated destination profiles that overlap with C2 timing. AEGIS distinguishes these via contextual rarity and protocol metadata, but periodic benign traffic remains a natural ambiguity source in enterprise environments.
4. **No Real-World Threat Attribution**: Classification as `BOTNET_C2_BEACONING` or `SUSPICIOUS_ENCRYPTED_TRAFFIC` indicates forensic behavior consistent with attack patterns, not legal attribution to specific threat actors.

---

## 10. Observable Metadata to Threat Evidence Pipeline

For every protocol indicator used in AEGIS, feature extraction and threat fusion follow an explainable path without payload inspection:

| Signal Type | Observable Network Input | Extracted Passive Feature | Detector Evidence Output | Threat Fusion Contribution |
| :--- | :--- | :--- | :--- | :--- |
| **Timing** | Packet Arrival Timestamps | IAT sequence, CV, Multi-lag Autocorr, FFT power | `timing_evidence_quality`, `best_autocorrelation` | Forms primary temporal beaconing confidence |
| **Destination** | IP Header (`dst_ip`, `dst_port`) | Host-pair concentration, destination count | `destination_persistence_score`, `destination_rarity_confirmed` | Distinguishes persistent endpoints from rotating pools |
| **TLS** | ClientHello Header | JA3/JA4 hash, SNI presence, cipher suite count | `has_suspicious_ja3`, `is_direct_ip_tls` | Provides independent cryptographic framework fingerprint |
| **QUIC** | UDP 443 Datagram Headers | Long header flag, QUIC version, SPLT lengths | `has_quic`, `is_initial_seen`, `length_variance` | Detects encrypted QUIC C2 / tunneling channels |
| **DNS** | DNS Request/Response (UDP 53) | Query domain string, RR type (TXT/NULL) | `domain_entropy`, `ml_probability`, `subdomain_entropy` | Correlates algorithmic DGA rendezvous or DNS tunneling |
| **Flow Volume** | Transport Layer Byte Counters | Forward bytes, backward bytes, byte ratio | `outbound_inbound_ratio`, `outbound_rate_bytes_per_sec` | Identifies asymmetric exfiltration or scanning sweeps |

